# Cross-feature components

This directory is intentionally empty. Anything shared by 2+ features (and not part of the design system) lands here. Feature-owned components live under `src/features/<feature>/components/`.
